info:
  name: login_existing
  type: http
  seq: 10

http:
  method: POST
  url: https://localhost:7259/login
  body:
    type: json
    data: |-
      {
        "username": "test",
        "password": "test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
