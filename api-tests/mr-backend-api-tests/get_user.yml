info:
  name: get_user
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:7259/user
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
