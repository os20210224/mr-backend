info:
  name: post_user
  type: http
  seq: 9

http:
  method: POST
  url: https://localhost:7259/user
  body:
    type: json
    data: |-
      {
        "name": "api test",
        "username": "test",
        "password": "test",
        "email": "test@test.test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
