info:
  name: get_throw
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:7259/throw
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
