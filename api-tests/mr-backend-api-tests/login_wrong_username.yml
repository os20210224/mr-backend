info:
  name: login_wrong_username
  type: http
  seq: 12

http:
  method: POST
  url: https://localhost:7259/login
  body:
    type: json
    data: |-
      {
        "username": "ne test",
        "password": "test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
